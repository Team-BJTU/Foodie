package nourriture.teambjtu.com.foodie;

import android.app.ProgressDialog;
import android.os.AsyncTask;

/**
 * Created by diaopie on 31/03/15.
 */
/*public class NetworkAsyncTask extends AsyncTask<Void, Void, Void> {

    private final ProgressDialog dialog = new ProgressDialog(CreateAccountActivity);

    protected void onPreExecute() {
        this.dialog.setMessage("Logging ...");
        this.dialog.show();
    }

    protected Void doInBackground(final Void... unused) {
        String showResult = new CreateAccountActivity().doRegister();
                //doAuthentication(username, password);
        System.out.println("[RESULTS =====> "  + showResult);

        return null;
    }

    protected void onPostExecute(Void result) {
        if (this.dialog.isShowing()) {
            this.dialog.dismiss();
        }
    }
}*/
